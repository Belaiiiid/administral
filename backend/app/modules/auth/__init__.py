"""Authentication & authorisation — the cross-cutting security foundation.

JWT bearer tokens with a short expiry, two primary roles (CITIZEN, AGENT), and
role guards that protect access to the dossiers. Adapted from the `MonParcours`
auth contribution into this project's module architecture.
"""
