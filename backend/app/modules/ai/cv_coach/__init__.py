"""CV coaching for France Travail: a conversational coach plus a one-shot
CV reviewer, both producing structured feedback (strengths / gaps / advice)
rather than a generated CV document — the citizen stays the author. Stateless
like `ai.job_match`: no `Application`, no persistence.
"""
