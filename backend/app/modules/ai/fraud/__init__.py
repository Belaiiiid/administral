"""Agent C4 — document metadata forensics (falsification detection).

Ported from the `metadata-extractor` contribution. Deterministic signal rules
plus an optional Mistral forensic verdict. Produces the fraud-signal stage of
the document pipeline.
"""
