"""Feature packages — self-contained vertical slices.

Each feature owns its routers / services / schemas / repositories / models,
mirroring the frontend `src/features/*` layout. The first slice living here is
`citizen.profiling` (the adaptive APL profiling assistant).
"""
