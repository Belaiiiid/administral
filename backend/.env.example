# Copy to `.env` and fill in.  `.env` is gitignored and must never be committed.
#
#   cp .env.example .env

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=monparcours
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password

# Optional override. Leave unset and the URL is assembled from the parts above
# (see app/core/config.py), which keeps the password in exactly one place.
# Set it only for a connection the parts cannot express — a socket path, SSL
# options, a managed-database URL.
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
APP_ENV=development
API_PORT=8000

# Comma-separated browser origins allowed to call the API.
# 5173 is the Vite dev server.
CORS_ORIGINS=http://localhost:5173

# ---------------------------------------------------------------------------
# AI / Mistral  (optional)
# ---------------------------------------------------------------------------
# Used by the coherence analyser (modules/ai/coherence). Leave unset for local
# development: analysis then degrades to "a_revoir" (to review) rather than
# declaring a dossier coherent without verification. Never commit a real key.
# MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-medium-latest
