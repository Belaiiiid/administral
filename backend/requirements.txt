# MonParcours — API dependencies.
# Install:  pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM + PostgreSQL driver
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Document text extraction
python-multipart==0.0.20
pypdf==5.1.0
pdfplumber==0.11.5

# Schema migrations
alembic==1.14.0

# Configuration and validation
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Citizen profiling assistant — adaptive agent loop (A3)
langgraph==0.2.62

# Citizen AI Assistant — migrated APL RAG (hybrid BM25 + Qdrant, LangGraph).
# The embeddings model (paraphrase-multilingual-MiniLM-L12-v2) is downloaded by
# sentence-transformers on first use and cached. torch/transformers install as
# sentence-transformers dependencies.
rank-bm25==0.2.2
qdrant-client==1.18.0
sentence-transformers==5.6.1
mistralai==2.7.1

# Tests
pytest==8.3.4
httpx==0.28.1

# Authentication (JWT + password hashing)
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
email-validator==2.2.0
